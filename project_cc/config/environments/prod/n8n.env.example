# config/environments/prod/n8n.env.example
#
# Purpose:
# Example environment variables for running n8n workflows in PROD.
# This file documents required variables and expectations.
#
# RULES:
# - Never commit real secrets.
# - PROD is authoritative and irreversible.
# - All side effects are real.
#
# Copy to `n8n.env` (or inject via your secret manager) before deployment.

# -------------------------------------------------------------------
# Core Environment Identity
# -------------------------------------------------------------------

# Logical environment identifier consumed by workflows
CC_ENV=prod

# Optional human-readable name for logs
CC_ENV_NAME=production

# -------------------------------------------------------------------
# n8n Runtime
# -------------------------------------------------------------------

# Public base URL where n8n is reachable (used for webhooks)
N8N_BASE_URL=https://n8n.adbyrd.com

# Node environment
NODE_ENV=production

# -------------------------------------------------------------------
# HubSpot (System of Record / Memory)
# -------------------------------------------------------------------

# Base API URL (do not change unless HubSpot changes)
HS_BASE=https://api.hubapi.com

# HubSpot Private App token (PROD)
# Stored in secret manager — placeholder only
HS_TOKEN=pat-PROD-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# PROD allows real writes
HS_WRITE_MODE=full

# -------------------------------------------------------------------
# Skool / Membership Sync
# -------------------------------------------------------------------

# Authoritative membership sync endpoint
SKOOL_SYNC_BASE=https://skool-sync.prod.internal

# Enable membership sync writes
SKOOL_SYNC_WRITE_ENABLED=true

# -------------------------------------------------------------------
# Generation / AI Vendors
# -------------------------------------------------------------------

# Script generation service base URL
GEN_BASE=https://gen.prod.internal

# Optional model hint for auditability
GEN_MODEL_HINT=llm-script-gen-prod

# -------------------------------------------------------------------
# Render / Asset Pipeline
# -------------------------------------------------------------------

# Video render service base URL
RENDER_BASE=https://render.prod.internal

# Render timeout override (ms)
RENDER_TIMEOUT_MS=120000

# -------------------------------------------------------------------
# Email Delivery (Postmark)
# -------------------------------------------------------------------

# Postmark API base
POSTMARK_BASE=https://api.postmarkapp.com

# Postmark Server API token (PROD)
# Stored in secret manager — placeholder only
POSTMARK_TOKEN=POSTMARK_PROD_SERVER_TOKEN

# Default outbound message stream
POSTMARK_MESSAGE_STREAM=outbound

# In PROD, sends are real
POSTMARK_SEND_ENABLED=true

# -------------------------------------------------------------------
# Rate Limiting & Safety
# -------------------------------------------------------------------

# Global throttle multiplier (1.0 = normal speed)
CC_THROTTLE_MULTIPLIER=1.0

# Maximum parallel executions allowed
CC_MAX_PARALLEL_RUNS=10

# -------------------------------------------------------------------
# Observability & Logging
# -------------------------------------------------------------------

# Log level hint
CC_LOG_LEVEL=info

# Disable verbose timing logs in prod unless debugging
CC_LOG_TIMINGS=false

# -------------------------------------------------------------------
# Feature Flags (documented in /config/feature-flags.yaml)
# -------------------------------------------------------------------

FEATURE_DELIVERY_SPINE_ENABLED=true
FEATURE_SKOOl_SYNC_ENABLED=true
FEATURE_EXPERIMENTAL_RENDERER=false

# -------------------------------------------------------------------
# Notes
# -------------------------------------------------------------------
# - All tokens here must be rotated via secret manager, not git.
# - Disabling POSTMARK_SEND_ENABLED in prod is a break-glass action only.
# - Any change to this file is a deployment event.
#
# If a variable is not listed here, it should not be required by n8n.
